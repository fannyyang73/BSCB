% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize_function.R
\name{find_global_maximum_h_all}
\alias{find_global_maximum_h_all}
\title{Find the global maximum of T(x) analytically via polyroot (Liu's method)}
\usage{
find_global_maximum_h_all(a, b, d, cov_mat)
}
\arguments{
\item{a}{Numeric. Left endpoint of the search interval.}

\item{b}{Numeric. Right endpoint of the search interval.}

\item{d}{Numeric vector of length \eqn{p+1}. Direction vector
(\eqn{\theta - \mu^*}).}

\item{cov_mat}{Numeric matrix of dimension \eqn{(p+1) \times (p+1)}.
Covariance matrix.}
}
\value{
A list with components \code{maximum}, \code{x_max}, and
\code{all_candidates}.
}
\description{
Computes \eqn{\sup_{x \in [a,b]} T(x)} by finding the stationary points of
\eqn{h(x) = T(x)^2 = N(x)/D(x)} via \code{polyroot}, where \eqn{N(x)} and
\eqn{D(x)} are polynomials. This is the recommended method.
}
