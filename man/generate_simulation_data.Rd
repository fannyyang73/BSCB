% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_data.R
\name{generate_simulation_data}
\alias{generate_simulation_data}
\title{Generate simulation datasets for polynomial regression}
\usage{
generate_simulation_data(
  p,
  n,
  e_sd,
  theta_true,
  a = -5,
  b = 5,
  replication = 2,
  design_index = 2,
  center_index = 1,
  n_ES_x = n,
  n_DO_init_x = 3e+05,
  AR_index = 0,
  rho = 0.1,
  batch_index = 1,
  seed = NULL
)
}
\arguments{
\item{p}{Integer. Polynomial degree. Must be 1, 2, or 3.}

\item{n}{Integer. Sample size.}

\item{e_sd}{Numeric. Error standard deviation (sigma in the paper).}

\item{theta_true}{Numeric vector of length \code{p + 1}. True regression
coefficients.}

\item{a}{Numeric. Left endpoint of the covariate domain \eqn{[a, b]}.}

\item{b}{Numeric. Right endpoint of the covariate domain \eqn{[a, b]}.}

\item{replication}{Integer. Number of simulation replications.}

\item{design_index}{Integer. Design type:
\code{1} = equally-spaced (ES);
\code{2} = D-optimal (DO).}

\item{center_index}{Integer. Centering of covariates:
\code{1} = mean-centred (default);
\code{0} = uncentred;
\code{2} = standardised.}

\item{n_ES_x}{Integer. Number of equally-spaced design points.
Only used when \code{design_index = 1}.}

\item{n_DO_init_x}{Integer. Candidate pool size for D-optimal search.
A large value (e.g. 300000) ensures that 6 support points are selected.
Only used when \code{design_index = 2}.}

\item{AR_index}{Integer. Error structure:
\code{0} = i.i.d. (default); \code{1} = AR(1).}

\item{rho}{Numeric. AR(1) coefficient. Only used when \code{AR_index = 1}.}

\item{batch_index}{Integer. Batch index used as part of the random seed
(\code{set.seed(1000 * batch_index + i)} for replication \code{i}).}

\item{seed}{Integer or \code{NULL}. Base random seed used internally for
the D-optimal design search (\code{OptimalDesign::od_KL}). If \code{NULL}
(default), no seed is set and results may vary between runs. When
\code{seed} is supplied, results are fully reproducible because the
internal search is stopped after a fixed number of restarts
(\code{rest.max}), rather than after a fixed time budget.}
}
\value{
A list containing:
\describe{
\item{X}{Design matrix of dimension \eqn{n \times (p+1)}.}
\item{Y.list}{List of \code{replication} response vectors, each of
length \code{n}.}
\item{optimal_x}{Vector of selected support points.}
\item{optimal_weights}{Vector of observation counts at each support
point.}
}
}
\description{
Generates a fixed design matrix X and a list of response vectors Y for
use in simulation studies of Bayesian simultaneous credible bands.
The design can be either equally-spaced (ES) or D-optimal (DO).
}
\examples{

\donttest{
# Example 1: quadratic model, D-optimal design
sim_data <- generate_simulation_data(
  p           = 2,
  n           = 20,
  e_sd        = 0.2,
  theta_true  = c(-6, -3, 0.25),
  a           = -5,
  b           =  5,
  replication = 1,
  design_index = 2,
  center_index = 1
)

X      <- sim_data$X
Y.list <- sim_data$Y.list
}

\donttest{
# Example 2: cubic model, equally-spaced design
sim_data2 <- generate_simulation_data(
  p            = 3,
  n            = 20,
  e_sd         = 0.2,
  theta_true   = c(1, 2, -1, 0.5),
  a            = -5,
  b            =  5,
  replication  = 1,
  design_index = 1,
  center_index = 1
)
}
}
