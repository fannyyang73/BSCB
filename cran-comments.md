## R CMD check results
0 errors | 2 warnings | 1 note

## Warnings

### checkbashisms
"A complete check needs the 'checkbashisms' script" appears because 
checkbashisms is not available on the local macOS environment. The 
cleanup and cleanup.win scripts use standard POSIX sh syntax and are 
expected to pass on CRAN's Linux servers.

### Compilation flags
The non-portable flags (-Wno-deprecated-declarations, etc.) are 
automatically generated by CmdStan during Stan model compilation and 
are not under the package author's control. This is a known issue with 
packages using cmdstanr/instantiate.

## Note

### unable to verify current time
This note is due to a local network issue and is unrelated to the 
package itself.

## Test environments
- macOS (local), R 4.4.x
- win-builder (R-devel)

## Downstream dependencies
None (new submission)
